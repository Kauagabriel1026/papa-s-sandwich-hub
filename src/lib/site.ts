/**
 * Dados de contato e identidade da loja.
 *
 * Mude o número AQUI e ele muda no site inteiro (cabeçalho, hero, rodapé e
 * botão de enviar pedido). Antes ele estava escrito à mão em 4 lugares.
 */

export const STORE_NAME = "PapaLéguas Burguer";

/** Formato internacional, só dígitos — é o que a API do wa.me espera. */
export const WHATSAPP_NUMBER = "5562995513839";

/** Como o número aparece para o cliente. */
export const PHONE_DISPLAY = "(62) 99551-3839";

export const WHATSAPP_URL = `https://wa.me/${WHATSAPP_NUMBER}`;
export const TEL_URL = `tel:+${WHATSAPP_NUMBER}`;
